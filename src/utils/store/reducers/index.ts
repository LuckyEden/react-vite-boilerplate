import counterReducer, { setPokemonName } from "./pokemonReducer";
import dataReducer, { setData } from "./dataReducer";

export {
    setPokemonName,
    setData,
    
    dataReducer,
    counterReducer
}